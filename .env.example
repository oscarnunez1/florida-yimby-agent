# Copy to .env and fill in values
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override DB path (default: db.sqlite in project root)
# DB_PATH=db.sqlite

# Optional: override Flask port (default: 5000)
# FLASK_PORT=5000
